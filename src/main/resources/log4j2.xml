<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
    <!-- Define property for today’s date -->
    <Properties>
        <Property name="today">${date:yyyy-MM-dd}</Property>
    </Properties>

    <Appenders>

        <!-- ====================== -->
        <!-- Console Appender -->
        <!-- ====================== -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="%d{HH:mm:ss} [%t] %-5level %logger{36} - %msg%n"/>
        </Console>

        <!-- ====================== -->
        <!-- Info Log (non-error) -->
        <!-- ====================== -->
        <RollingFile name="InfoFile"
                     fileName="logs/testng-info.log"
                     filePattern="logs/testng-info-%d{yyyy-MM-dd}.log"
                     append="true">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} [%t] %-5level %logger{36} - %msg%n"/>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
            </Policies>
            <Filters>
                <ThresholdFilter level="ERROR" onMatch="DENY" onMismatch="ACCEPT"/>
            </Filters>
        </RollingFile>

        <!-- ====================== -->
        <!-- Error Log -->
        <!-- ====================== -->
        <RollingFile name="ErrorFile"
                     fileName="logs/testng-error.log"
                     filePattern="logs/testng-error-%d{yyyy-MM-dd}.log"
                     append="true">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} [%t] %-5level %logger{36} - %msg%n"/>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
            </Policies>
            <Filters>
                <ThresholdFilter level="ERROR" onMatch="ACCEPT" onMismatch="DENY"/>
            </Filters>
        </RollingFile>

        <!-- ====================== -->
        <!-- Full Combined Log -->
        <!-- ====================== -->
        <RollingFile name="DailyFullLog"
                     fileName="logs/${today}/full.log"
                     filePattern="logs/${today}/full-%i.log"
                     append="true">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} [%t] %-5level %logger{36} - %msg%n"/>
            <Policies>
                <SizeBasedTriggeringPolicy size="100MB"/>
            </Policies>
        </RollingFile>

    </Appenders>

    <Loggers>
        <Root level="debug">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="InfoFile"/>
            <AppenderRef ref="ErrorFile"/>
            <AppenderRef ref="DailyFullLog"/>
        </Root>
    </Loggers>
</Configuration>
